import React, {useState} from "react"

const Progress = ({children}) => {

    return (
        <div id="progress">
            <div id="current"></div>
        </div>
    )
}

export default Progress;